<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.aura.aurastay.repository.RoomImageRepository">
    <select id="saveRoomImage" resultType="RoomImageDTO">
        SELECT image_no, filename, filepath, accommodation_no
        FROM room_image
    </select>

    <insert id="add" parameterType="RoomImageDTO">
        INSERT INTO room_image (
        filename,
        filepath,
        accommodation_no
        ) VALUES (
        #{filename},
        #{filepath},
        #{acmNo}  <!-- 숙소 번호 -->
        )
    </insert>
    <select id="getImages" parameterType="int" resultType="RoomImageDTO">
        SELECT
        r.image_no AS imageNo,
        r.filename AS filename,
        r.filepath AS filepath
        FROM room_image r
        WHERE r.accommodation_no = #{acmNo}
    </select>

</mapper>