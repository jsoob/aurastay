<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.aura.aurastay.repository.MemberRepository">
    <!-- 회원가입 -->
    <insert id="insertMember" parameterType="MemberDTO">
        insert into member
        values(null,#{memberEmail},#{providerId},#{memberPassword},#{memberName},#{memberNickname},#{memberPhoneNumber},0,current_timestamp(),null,#{authority})
    </insert>
    <!-- 일치하는 이메일이 있는지 확인 -->
    <select id="findByUsername" parameterType="String" resultType="MemberDTO">
        select member_no as memberNo, member_email as memberEmail, provider_id as providerId, member_password as memberPassword, member_name as memberName,member_nickname as memberNickname, member_phone_number as memberPhoneNumber, point, registration_date as registrationDate, withdrawal_date as withdrawalDate, authority
        from member
        where member_email = #{username} and provider_id is null
    </select>
    <!-- 이미 존재하는 이메일인지 확인 -->
    <select id="existsByEmail" parameterType="String" resultType="boolean">
        SELECT EXISTS (SELECT 1 FROM member WHERE member_email = #{username} and provider_id is null)
    </select>
    <!-- providerid로 사용자 조회 -->
    <select id="findByProviderId" parameterType="String" resultType="MemberDTO">
        select member_no as memberNo, member_email as memberEmail, provider_id as providerId, member_password as memberPassword, member_name as memberName,member_nickname as memberNickname, member_phone_number as memberPhoneNumber, point, registration_date as registrationDate, withdrawal_date as withdrawalDate, authority
        from member
        where provider_id = #{providerId}
    </select>
    <!-- 소셜로그인 기존 사용자는 업데이트 -->
    <update id="updateMember" parameterType="MemberDTO">
        update member
        set member_email=#{memberEmail}, member_nickname=#{memberNickname}
        where provider_id = #{providerId}
    </update>
    <!-- 비밀번호 재설정 -->
    <update id="resetPassword" parameterType="MemberDTO">
        update member
        set member_password = #{memberPassword}
        where member_email = #{memberEmail} and provider_id is null
    </update>
</mapper>
