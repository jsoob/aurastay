<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.aura.aurastay.repository.ReservationCancelRepository">
    <insert id="cancelReservationReq" parameterType="ReservationCancelDTO">
        INSERT INTO
            reservation_cancel (cancel_reasons, cancel_status, cancel_date, reservation_no)
        VALUES
            ( #{cancelReasons}, #{cancelStatus}, SYSDATE(), #{reservationNo} )
    </insert>

    <select id="getRsCancl" parameterType="int" resultType="ReservationCancelDTO">
        SELECT
            rc.cancel_no as cancelNo, rc.cancel_reasons as cancelReasons,
            rc.cancel_status as cancelStatus,
            STR_TO_DATE(rc.cancel_date, '%Y-%m-%d') as cancelDate,
            STR_TO_DATE(rc.cancel_resp_date, '%Y-%m-%d') as cancelRespDate,
            rc.reservation_no as reservationNo
        FROM reservation_cancel rc
        WHERE rc.reservation_no = #{reservationNo}
    </select>
</mapper>