<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.aura.aurastay.repository.CategoryRepository">
    <!-- 모든 카테고리 조회 -->
    <select id="getAllCategories" resultType="kr.co.aura.aurastay.dto.CategoryDTO">
        SELECT category_no, category_name
        FROM category
    </select>
</mapper>