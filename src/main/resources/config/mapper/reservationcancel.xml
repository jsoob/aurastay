<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.aura.aurastay.repository.ReservationCancelRepository">
    <insert id="cancelReservationReq" parameterType="ReservationCancelDTO">
        INSERT INTO
            reservation_cancel (cancel_reasons, cancel_status, cancel_date, reservation_no)
        VALUES
            ( #{cancelReasons}, #{cancelStatus}, SYSDATE(), #{reservationNo} )
    </insert>
</mapper>